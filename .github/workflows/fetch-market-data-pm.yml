name: 获取AI板块数据（午报预取）
run-name: afternoon fetch ${{ inputs.request_id || github.run_id }}

on:
  push:
    branches: [main]
    paths:
      - 'stock_report/triggers/afternoon.json'
  schedule:
    # 12:00 CST 午间快照：上午收盘（11:30）后半小时，此时当日半天行情已定。
    # 作用是给午报一份**保底的当日数据**——13:50 那次万一失败，午报不至于
    # 退回昨收（云端连不上实时源，退回昨收就是整篇报告错一天）。
    - cron: '0 4 * * 1-5'   # 12:00 CST Mon-Fri 午间快照
    - cron: '50 5 * * 1-5'  # 13:50 CST Mon-Fri 盘中预取
  workflow_dispatch:
    inputs:
      request_id:
        description: 'Correlation ID supplied by the report routine'
        required: false
        type: string

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      # 迟到的定时抓数不许覆盖已投递的快照，理由同 fetch-market-data.yml。
      # routine 主动 push trigger 的那次必须照跑。
      - name: Check whether today's report already shipped
        id: guard
        run: |
          if [ "${{ github.event_name }}" = "schedule" ]; then
            state=$(python -m stock_report.pipeline_state --mode afternoon --check-delivered)
          else
            state=pending
          fi
          echo "delivered=$state" >> "$GITHUB_OUTPUT"
          echo "today's afternoon delivery: $state"
      - if: steps.guard.outputs.delivered != 'sent'
        run: python fetch_market_data_pm.py
      - name: Stamp connector request
        if: github.event_name == 'push'
        run: |
          python -m stock_report.connector_trigger \
            --mode afternoon \
            --snapshot stock_report/data/afternoon_latest.json \
            --trigger stock_report/triggers/afternoon.json \
            --trigger-commit-sha "$GITHUB_SHA"
      - name: Commit data
        if: steps.guard.outputs.delivered != 'sent'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add stock_report/data/afternoon_latest.json
          git diff --cached --quiet || git commit -m "data: afternoon market data $(date -u +%Y-%m-%d)"
          git pull --rebase
          git push
